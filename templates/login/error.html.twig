<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Accesso Negato - Simulatore CFU UniMarconi</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(0deg, #379975 40px, #FFF 0%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #444444;
        }
        
        .error-container {
            background: white;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.1);
            text-align: center;
            max-width: 500px;
            width: 90%;
        }
        
        .error-icon {
            font-size: 4rem;
            color: #E57552;
            margin-bottom: 20px;
        }
        
        h1 {
            color: #E57552;
            margin-bottom: 20px;
            font-size: 1.8rem;
        }
        
        .error-message {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 5px;
            border-left: 4px solid #E57552;
            margin-bottom: 30px;
            font-size: 1.1rem;
        }
        
        .login-link {
            display: inline-block;
            background: #379975;
            color: white;
            padding: 12px 30px;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            transition: background 0.3s;
        }
        
        .login-link:hover {
            background: #2d7a5f;
        }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-icon">🔒</div>
        <h1>Accesso Negato</h1>
        <div class="error-message">
            {{ error }}
        </div>
        <p>Per accedere al sistema, è necessario un token valido.</p>
        <br>
        <a href="/login?token=demo123456" class="login-link">Accedi con Token Demo</a>
    </div>
</body>
</html>
