<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Accesso Negato - Simulatore CFU UniMarconi</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(0deg, #379975 40px, #FFF 0%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #444444;
        }
        
        .error-container {
            background: white;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.1);
            text-align: center;
            max-width: 500px;
            width: 90%;
        }
        
        .error-icon {
            font-size: 4rem;
            color: #E57552;
            margin-bottom: 20px;
        }
        
        h1 {
            color: #E57552;
            margin-bottom: 20px;
            font-size: 1.8rem;
        }
        
        .error-message {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 5px;
            border-left: 4px solid #E57552;
            margin-bottom: 30px;
            font-size: 1.1rem;
        }
        
        .error-details {
            color: #666;
            margin-bottom: 30px;
            line-height: 1.6;
        }
        
        .error-code {
            background: #f8f9fa;
            padding: 10px;
            border-radius: 5px;
            font-family: monospace;
            color: #E57552;
            font-weight: bold;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-icon">🔒</div>
        <h1>Accesso Negato</h1>
        <div class="error-message">
            Non hai i permessi necessari per accedere a questa risorsa.
        </div>
        <div class="error-details">
            <p>Per accedere al sistema è necessario:</p>
            <ul style="text-align: left; margin: 20px 0;">
                <li>Un token di autenticazione valido</li>
                <li>Il ruolo di segreteria</li>
                <li>Una sessione attiva</li>
            </ul>
            <p>Contatta l'amministratore di sistema se ritieni che questo sia un errore.</p>
        </div>
        <div class="error-code">
            Errore 403 - Forbidden
        </div>
    </div>
</body>
</html>
